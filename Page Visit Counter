<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Page Visit Counter</title>
	<script src="https://cdn.tailwindcss.com"></script>
	<style>
		body {
			font-family: 'Inter', sans-serif;
		}
	</style>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-4">

	<div class="bg-white p-8 md:p-12 rounded-2xl shadow-xl max-w-lg w-full text-center transform transition-all duration-300 hover:scale-105">
		<h1 class="text-3xl md:text-4xl font-extrabold text-gray-800 mb-4 tracking-tight">Welcome</h1>
		<p id="visit-count" class="text-6xl md:text-8xl font-black text-blue-600 mb-6 transition-transform duration-500 ease-out transform scale-100">
		</p>
		<p class="text-lg text-gray-600">
			This page has been opened this many times on this device.
		</p>
	</div>
	
	<script>
		function updateVisitCount() {
			let visitCount = parseInt(localStorage.getItem('page_visit_count')) || 0;
			visitCount++;
			localStorage.setItem('page_visit_count', visitCount);
			const countElement = document.getElementById('visit-count');
			countElement.textContent = visitCount;
		}
		
		window.onload = updateVisitCount;
	</script>
</body>
</html>
